[package]
name = "enclavekit-encoding"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[features]
default = ["sha2"]
sha2 = ["dep:sha2"]

[dependencies]
borsh = { version = "1.8.1", features = ["derive"] }
sha2 = { version = "0.11.0", optional = true }